# Implementation status

A separate `feature/unified-auth` branch has been created from `feature/chat-notifications-profile`.

The coordinator is intentionally not wired into the UI yet: the existing admin repository contract and navigation/session lifecycle must be verified first to avoid introducing a build or authentication regression.
